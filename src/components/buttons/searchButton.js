import React from "react";
import classes from './searchButton.scss';

const searchButton = () => {
    return (
        <button className={classes.btn}>Ara</button>
    );
};

export default searchButton;